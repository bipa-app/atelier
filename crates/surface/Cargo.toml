[package]
name = "atelier-surface"
version = "0.2.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The agent surface: MCP over stdio, thin over the atelier SDK"
readme = "../../README.md"
keywords = ["mcp", "agents", "workspace", "http"]
categories = ["development-tools"]

[lints]
workspace = true

[dependencies]
tiny_http.workspace = true
atelier-sdk = { version = "0.2.0", path = "../core" }
serde_json.workspace = true

[dev-dependencies]
tempfile.workspace = true
